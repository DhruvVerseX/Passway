# Root values used when running every app together locally.
PORT=4000

# API/backend values live in apps/api/.env.example.
# Dashboard-only public values live in apps/dashboard/.env.example.
# Marketing web public values live in apps/web/.env.example.

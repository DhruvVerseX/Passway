import Link from "next/link";
import { ArrowRight, KeyRound, Network, ScrollText } from "lucide-react";
import { Code, Doc, Section } from "@/components/doc";
export default function DocsHome(){return <Doc eyebrow="ENVVAULT DOCS" title="Secrets at runtime, without the .env handoff." description="Passway EnvVault stores project secrets behind a revokable token and loads them into your application when it starts."><Section title="Start here"><div className="grid gap-3 sm:grid-cols-3">{[[KeyRound,"Create a token"],[Network,"Set access rules"],[ScrollText,"Review every fetch"]].map(([Icon,label])=>{const C=Icon as typeof KeyRound;return <div key={label as string} className="rounded-xl border border-[#1D2633] bg-[#0B0F14] p-4"><C className="text-blue-400" size={18}/><b className="mt-4 block text-sm text-[#E6EDF3]">{label as string}</b></div>})}</div></Section><Section title="Small integration surface"><p>Install the SDK, provide your project token through the deployment environment, and load secrets before application initialization.</p><Code>{`import { loadEnv } from "@passway/envvault";\n\nawait loadEnv();\n\nconsole.log(process.env.DATABASE_URL);`}</Code></Section><Link href="/quickstart" className="inline-flex items-center gap-2 text-blue-400">Follow the quickstart <ArrowRight size={16}/></Link></Doc>;}

import { Doc, Section } from "@/components/doc";
const errors=[["401","Invalid token","The token is missing, malformed, revoked, or unknown."],["403","IP blocked","The request origin is outside the project allowlist."],["429","Rate limited","The token exceeded its current request budget."],["5xx","Service error","The vault could not complete a valid request. Retry with backoff."]];
export default function Errors(){return <Doc eyebrow="SDK / REFERENCE" title="Errors" description="EnvVault fails closed. A rejected request never returns partial secret data."><Section title="HTTP responses"><div className="overflow-hidden rounded-xl border border-[#1D2633]">{errors.map(([code,name,body])=><div key={code} className="grid gap-2 border-b border-[#1D2633] bg-[#0B0F14] p-4 last:border-0 sm:grid-cols-[60px_130px_1fr]"><code className={code==="429"?"text-amber-400":"text-red-400"}>{code}</code><b className="text-[#E6EDF3]">{name}</b><span>{body}</span></div>)}</div></Section></Doc>;}

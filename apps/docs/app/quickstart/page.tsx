import { Code, Doc, Note, Section } from "@/components/doc";
export default function Quickstart(){return <Doc eyebrow="GETTING STARTED" title="Quickstart" description="Load a project vault into a Node.js process in a few minutes."><Section title="1. Install the SDK"><Code>npm install @passway/envvault</Code></Section><Section title="2. Add the project token"><p>Set the generated token in your deployment provider. Keep it out of source control.</p><Code>PASSWAY_TOKEN=evt_dev_your_token</Code></Section><Section title="3. Load the vault"><Code>{`import { loadEnv } from "@passway/envvault";\n\nawait loadEnv();`}</Code></Section><Note>EnvVault never needs raw secret values in your repository. Only the revokable project token is configured at runtime.</Note></Doc>;}

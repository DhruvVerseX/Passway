import { Network, Plus, Trash2 } from "lucide-react";
import { Badge, Button, Card } from "@/components/ui";
import { ProjectPageShell } from "@/components/project-page-shell";
const rules=[["Office network","103.21.58.0/24"],["Production VPC","10.0.0.0/16"],["Deploy runner","198.51.100.17/32"]];
export default async function IpRulesPage({params}:{params:Promise<{projectId:string}>}){const {projectId}=await params;return <ProjectPageShell projectId={projectId}><div className="mb-5 flex items-end justify-between"><div><h2 className="font-semibold">IP allowlist</h2><p className="mt-1 text-sm text-[#8B949E]">Only approved networks can request secrets.</p></div><Button><Plus size={16}/>Add rule</Button></div><Card><div className="divide-y divide-[#1D2633]">{rules.map(([name,cidr])=><div key={cidr} className="flex items-center gap-4 p-5"><span className="grid size-9 place-items-center rounded-lg bg-blue-500/10 text-blue-400"><Network size={16}/></span><span className="min-w-0 flex-1"><b className="block text-sm">{name}</b><code className="mono text-xs text-[#8B949E]">{cidr}</code></span><Badge tone="success">Active</Badge><button className="p-2 text-red-400" aria-label={`Delete ${name}`}><Trash2 size={16}/></button></div>)}</div></Card></ProjectPageShell>;}

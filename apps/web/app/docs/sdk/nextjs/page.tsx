import { Code, Doc, Note, Section } from "../../components/doc";
export default function NextSdk(){return <Doc eyebrow="SDK / NEXT.JS" title="Next.js integration" description="Load server-only secrets during application startup and keep them out of the client bundle."><Section title="Instrumentation hook"><Code>{`// instrumentation.ts\nexport async function register() {\n  if (process.env.NEXT_RUNTIME === "nodejs") {\n    const { loadEnv } = await import("@passway/envvault");\n    await loadEnv();\n  }\n}`}</Code></Section><Note>Never expose secret keys with a <code>NEXT_PUBLIC_</code> prefix. Next.js intentionally bundles those values for the browser.</Note></Doc>;}

import { Doc, Section } from "../components/doc";
export default function SecurityDocs(){return <Doc eyebrow="SECURITY MODEL" title="Security" description="A project request must pass every access layer before EnvVault returns a vault payload."><Section title="Access layers"><ol className="space-y-3">{["A valid, active project token","Token scope matches the requested project","Request IP matches the configured allowlist","Rate limits permit the request"].map((x,i)=><li key={x} className="flex gap-3 rounded-xl border border-[#1D2633] bg-[#0B0F14] p-4"><code className="text-blue-400">0{i+1}</code><span>{x}</span></li>)}</ol></Section><Section title="Operational guidance"><p>Rotate credentials at their source when exposure is suspected, then update the vault. Revoke the project token separately to stop EnvVault delivery immediately.</p></Section></Doc>;}

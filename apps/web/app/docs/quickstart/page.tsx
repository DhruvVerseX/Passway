import { Code, Doc, Note, Section } from "../components/doc";
export default function Quickstart(){return <Doc eyebrow="GETTING STARTED" title="Quickstart" description="Verify a hosted project environment from your Node.js process."><Section title="1. Install the CLI"><Code>npm install -g @passway/cli</Code></Section><Section title="2. Add the environment token"><p>Copy the token shown when you host the environment, then set it in your deployment provider. Keep it out of source control.</p><Code>PASSWAY_TOKEN=ps_live_&lt;issued_token&gt;</Code></Section><Section title="3. Verify the connection"><Code>passway start</Code></Section><Note>Passway tokens are revocable. The CLI verifies encrypted-secret delivery without printing secret values.</Note></Doc>;}
